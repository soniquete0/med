/// <reference types="react" />
export interface HamburgerProps {
    active: boolean;
    onClick: Function;
}
declare const Hamburger: (props: HamburgerProps) => JSX.Element;
export default Hamburger;
